const process = require('process')

const myNumber = parseInt(process.argv[2]);


if (myNumber === parseInt(Number(myNumber))) {
    console.log('My number:', (myNumber))
} else if (myNumber !== parseInt(Number())) {
    console.log('Not number')
}
